# 포트폴리오 랜딩 사이트 현행 기준서

## 문서 목적

이 문서는 신규 구축을 요청하기 위한 프롬프트가 아니라, 현재 구현된 사이트의 확정 사항과 향후 유지해야 할 기준을 기록한 문서다.

향후 수정 작업에서는 이 문서를 현재 상태의 기준점으로 사용한다. 실제 구현이나 사용자 승인 내용이 변경되면 필요한 시점 또는 사용자의 요청에 따라 이 문서도 함께 갱신한다.

## 작성 기준

- 저장소: `river121314/codex-site`
- 구현 기준 브랜치: `feat/header-scroll-interaction`
- 프로덕션 기준 브랜치: `main`
- 프로덕션 URL: <https://codex-site-delta.vercel.app>
- 사이트 구성: 별도 서버와 데이터베이스가 없는 정적 HTML, CSS, JavaScript 사이트
- 배포 방식: GitHub와 연동된 Vercel 배포
- 현재 작업 브랜치의 헤더 및 모바일 메뉴 변경은 프로덕션 `main`에 아직 병합되지 않은 상태다.

## 현재 구현 범위

- 페이지: 한국어 우선의 단일 페이지
- 섹션 순서:
  1. Hero
  2. About
  3. Projects — 카드 3개
  4. Additional Info
  5. Contact
- PC와 모바일 반응형 지원
- About의 프로필 사진 영역은 표시하지 않음
- 프로젝트 이미지는 포트폴리오 자료의 슬라이드 이미지를 사용

## 현재 구현 내용

### 공통 디자인

- 미니멀하고 차분한 모노톤을 유지한다.
- 배경은 따뜻한 흰색, 본문은 눈의 피로를 줄인 짙은 색을 사용한다.
- 강조 색상은 전문적인 블루 한 가지를 사용한다.
- 영문은 Inter, 한글은 Pretendard를 사용한다.
- 충분한 여백과 영화 엔딩 크레딧을 연상시키는 정돈된 구성을 유지한다.
- 화려한 애니메이션과 자동 재생 영상은 사용하지 않는다.
- 사용자의 모션 감소 설정을 지원한다.

### 헤더 및 탐색

- 좌측 워드마크는 `PORTFOLIO`로 표시한다.
- PC 메뉴는 `ABOUT`, `PROJECTS`, `CONTACT`로 구성한다.
- 헤더는 화면 상단에 고정하며 전체 너비의 배경과 하단 구분선을 사용한다.
- 아래로 스크롤하면 헤더를 숨기고, 위로 스크롤하면 헤더를 부드럽게 노출한다.
- 페이지 최상단에서는 헤더를 노출한다.
- 모바일에서는 PC 메뉴 대신 햄버거 버튼을 표시한다.
- 햄버거 버튼을 누르면 우측 서랍형 메뉴와 좌측 딤드 영역을 표시한다.
- 모바일 메뉴는 `ABOUT`, `PROJECTS`, `CONTACT`로 구성하며 메뉴별 숫자는 표시하지 않는다.
- 닫기 버튼, 딤드 영역, `Esc` 키로 모바일 메뉴를 닫을 수 있다.
- 모바일 메뉴를 선택하면 메뉴와 딤드 영역을 닫고 해당 섹션으로 이동한다.
- PC에서는 화면 우측에 `Hero`, `About`, `Projects`, `Additional`, `Contact`로 구성한 고정 섹션 인디케이터를 표시한다.
- 고정 섹션 인디케이터는 현재 섹션을 블루 색상과 긴 라인으로 표시하고, 마우스 오버 및 키보드 초점 시 텍스트와 라인을 강조한다.
- 비활성 항목도 짧은 라인과 비활성 색상의 섹션명을 항상 표시한다.
- 넓은 PC 화면에서는 인디케이터를 기존 위치보다 오른쪽으로 `40px` 이동해 콘텐츠와의 여백을 확보한다.
- 고정 섹션 인디케이터를 선택하면 해당 섹션으로 부드럽게 이동한다.
- 모바일에서는 고정 섹션 인디케이터를 숨기고 햄버거 메뉴로 탐색한다.

### Hero

- 서비스 기획자 포트폴리오임을 먼저 알린다.
- Hero 상단 라벨은 `SERVICE PLANNER`로 표시한다.
- Hero 상단 라벨의 글자 크기는 PC `14px`, 모바일 `12px`로 적용한다.
- Hero 메인 타이틀은 PC `70px`, 모바일 `42px`, `line-height: 1.30`, `font-weight: 800`으로 표시한다.
- Hero 설명 문구는 PC `17px`, 모바일 `15px`로 표시한다.
- 글자 크기 변경 후에도 Hero 콘텐츠의 기존 중앙 정렬 기준을 유지한다.
- Hero 우측의 원형 윤곽 오브젝트는 PC에서 화면 오른쪽으로 `100px` 더 이동하고 낮은 투명도의 선으로 표시하며, 모바일에서는 기존 위치를 유지한다.
- 최초 진입 시 상단 라벨, 메인 타이틀, 설명 문구, 스크롤 안내를 CSS 키프레임과 `180ms` 간격으로 순차 노출한다.
- 핵심 문구는 “고객의 요구가 실제 서비스로 이어지도록 끝까지 확인합니다.”이다.
- 블루 강조 문구에 빠른 타이핑 효과를 적용한다.
- About으로 이동하는 `SCROLL` 안내와 Google Material 아래 화살표를 제공한다.
- 스크롤 안내는 텍스트 아래에 화살표를 배치하고 Hero 하단의 페이지 수직 중심선에 맞춰 가운데 정렬한다.
- 스크롤 안내 묶음은 느린 상하 바운스 효과를 반복한다.

### About

- 프로필 사진 영역은 표시하지 않는다.
- 섹션 상단선과 콘텐츠 사이의 간격은 `80px`, 섹션 라벨과 메인 타이틀 사이는 `60px`, 메인 타이틀과 리드 문구 사이는 `40px`로 유지한다.
- 섹션 라벨은 PC `14px`, 모바일 `12px`, `line-height: 1.30`으로 표시한다.
- 메인 타이틀은 PC `70px`, 모바일 `42px`, `font-weight: 800`, `line-height: 1.30`으로 표시하며 `결과물`만 블루로 강조한다.
- 리드 문구는 PC `17px`, 모바일 `15px`로 표시한다.
- PC에서는 메인 타이틀과 리드 문구를 좌측에, 기획 원칙 카드 3개를 우측에 배치한다.
- 모바일에서는 소개 문구와 기획 원칙 카드를 위아래로 배치한다.
- 기획 원칙은 `사용자 경험`, `운영 조건`, `구현 가능성` 순서의 박스형 카드로 표시한다.
- 각 기획 원칙 카드는 블루 번호, 원칙명, 얇은 테두리의 Google Material 아이콘 박스로 구성한다.
- 기획 원칙 카드는 마우스 오버 시 테두리가 진해진다.
- 리드 문구는 “기획자는 문제만 해결하는 사람이 아닌, 서비스의 존재와 살아남아야 할 이유를 결과물로 증명하는 사람입니다.”이다.
- 경력과 보유 기술 위에는 가운데 정렬된 통합 서브 타이틀 `경력 · 스킬`을 표시한다.
- 통합 서브 타이틀과 경력·스킬 영역 사이는 `40px`로 유지한다.
- 경력 요약은 `18px`의 `총 8년 11개월`과 `12px`의 `서비스 기획 5년`으로 표시한다.
- 회사별 경력은 회사명 `14px`, 소속·역할과 담당 업무 `12px`, 재직 기간과 총 재직 개월 `11px`로 표시한다.
- PC 경력 항목은 첫 줄에 회사명·소속 역할·기간, 둘째 줄에 담당 업무를 표시한다.
- 모바일 경력 항목은 첫 줄에 회사명·기간, 둘째 줄에 소속 역할, 셋째 줄에 담당 업무를 표시한다.
- 경력 목록 우측에는 보유 기술을 배치하며, 모바일에서는 경력 아래에 이어서 배치한다.
- 학력 정보는 사이트에 표시하지 않고 이력서와 포트폴리오 문서에서 제공한다.
- 보유 기술은 별도 구분선 없는 밝은 배경의 박스에 `기획 산출물`, `설계·협업 도구`, `기술 이해`, `AI 활용`, `데이터 활용`만 표시한다.
- 데이터 활용은 사용자가 지정한 `GA4(세션 조회·기간별 비교·리포트 작성)`으로 표시한다.
- 현재 콘텐츠는 제공된 포트폴리오 자료 2~3번 슬라이드를 바탕으로 구성했다.

### Projects

- 프로젝트 카드 3개를 다음 순서로 표시한다.
  1. 아큐브 안경사 통합 사이트 개편
  2. 아큐브 가상 피팅 사이트 구축
  3. CJ 워니버스 홈페이지 구축
- 각 카드에는 기간, 프로젝트 유형, 요약, 담당 역할, 관련 이미지가 포함된다.
- 각 카드의 상세 영역에서 핵심 과제와 판단·실행 내용을 펼쳐 볼 수 있다.
- 하나의 프로젝트 상세 영역을 열면 기존에 열려 있던 다른 상세 영역은 닫는다.
- 현재 콘텐츠는 제공된 포트폴리오 자료 4~13번 슬라이드를 바탕으로 구성했다.

### Additional Info

- 기획 과정을 `정의 → 설계 → 검증` 순서로 표시한다.
- 요구사항, 정보구조도, 화면설계서, Figma, 테스트 케이스, QA 경험을 표시한다.
- 사용 도구와 기술을 별도 목록으로 표시한다.
- 현재 콘텐츠는 제공된 포트폴리오 자료 14번 슬라이드를 바탕으로 구성했다.

### Contact

- 현재 임시 연락처는 다음과 같다.
  - 이메일: `email@example`
  - 전화번호: `010-1234-5678`
- 이메일과 전화번호는 각각 메일 작성과 전화 연결 링크로 구현한다.
- 실제 입사 지원에 사용하기 전 사용자의 요청에 따라 최종 연락처로 교체한다.

### 인터랙션 및 접근성

- 섹션 요소는 스크롤하여 화면에 들어올 때 한 번만 부드럽게 나타난다.
- 앵커 메뉴는 해당 섹션으로 부드럽게 이동한다.
- 프로젝트 상세 영역은 기본 HTML 상세 요소를 사용한다.
- 본문 바로가기 링크, 탐색 및 메뉴 버튼의 접근성 이름, 키보드 메뉴 닫기를 제공한다.
- 모션 감소 환경에서는 타이핑, 스크롤 이동 및 전환 효과를 최소화한다.

### 검색 노출 및 배포

- HTML 문서에 `noindex, nofollow, noimageindex` 메타 태그를 적용한다.
- 모든 Vercel 응답에 `X-Robots-Tag: noindex, nofollow, noimageindex` 헤더를 적용한다.
- 검색 차단 설정은 검색엔진 노출 제한을 위한 것이며 공개 GitHub 저장소의 열람이나 복제를 제한하지 않는다.
- 일반 수정은 별도 기능 브랜치에서 작업하고 Vercel Preview로 확인한 뒤, 사용자의 명시적 승인 후 `main`에 병합하여 프로덕션에 반영한다.

## 유지해야 할 사항

- 사용자가 승인한 콘텐츠와 사용자가 지정한 임시 연락처
- `Hero → About → Projects → Additional Info → Contact` 섹션 순서
- 미니멀하고 차분한 모노톤, 따뜻한 흰색 배경, 짙은 본문색, 단일 블루 강조색
- Inter와 Pretendard 글꼴 조합
- 파비콘은 `PORTFOLIO`를 의미하는 `P` 문자, 짙은 배경, 따뜻한 흰색 문자와 블루 포인트로 구성한다.
- 충분한 여백과 절제된 인터랙션
- PC와 모바일에서 내용이 깨지지 않는 반응형 구성
- 모바일의 햄버거 버튼과 우측 서랍형 메뉴 방식
- PC 우측의 현재 위치 표시 및 앵커 이동용 고정 섹션 인디케이터
- 프로필 사진 Placeholder
- 공개 가능한 개인정보는 사용자가 명시한 임시 이메일과 전화번호로 제한
- 검색엔진 노출을 제한하는 `noindex` 설정
- 섹션별 수정, Preview 확인, 승인 후 프로덕션 반영 절차

## 미확정 사항

- Hero를 포함한 각 섹션의 문구, 정보 배치 및 세부 스타일은 앞으로 섹션별 검토가 필요하다.
- About의 프로필 사진과 최종 소개 문구는 확정되지 않았다.
- Projects 카드 3개의 문구, 이미지, 상세 정보와 공개 범위는 최종 확정되지 않았다.
- Additional Info의 문구, 도구 목록 및 기술 표기 범위는 최종 확정되지 않았다.
- Contact의 이메일과 전화번호는 임시 값이며 입사 지원 전에 교체해야 한다.
- 실제 지원용 프로덕션 공개 시점과 `noindex` 해제 여부는 확정되지 않았다.
- 공개 GitHub 저장소의 Private 전환 여부는 확정되지 않았다.

## 변경 원칙

- 기존 구현을 기본값으로 유지한다.
- 사용자가 명시한 섹션과 항목만 수정한다.
- 요청하지 않은 문구, 구조, 스타일은 변경하지 않는다.
- 변경 전 영향받는 범위를 먼저 설명한다.
- 한 번에 한 섹션씩 수정하고 확인받는다.
- 코드와 콘텐츠는 제공된 자료 및 사용자가 확인한 사실만 사용하며, 경력·성과·수치·역할을 임의로 추가하지 않는다.
- 수정은 별도 브랜치에서 진행하고 Preview로 확인한다.
- 사용자의 명시적 승인 전에는 수정 브랜치를 `main`에 병합하지 않는다.
- 승인된 변경이 실제 구현에 반영되면 이 문서의 관련 기준도 함께 검토한다.
